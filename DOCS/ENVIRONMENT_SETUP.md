# ⚙️ ENVIRONMENT SETUP & DEPENDENCIES

> **MODERN DEEP LEARNING STACK FOR FACE RESTORATION, SUPER-RESOLUTION, AND IDENTITY PRESERVATION**

---

## 🖥 SYSTEM REQUIREMENTS

### MINIMUM
- PYTHON 3.10
- CUDA-CAPABLE GPU (RECOMMENDED)
- NVIDIA DRIVER + CUDA TOOLKIT
- 16GB RAM

### RECOMMENDED
- NVIDIA RTX GPU (8GB+ VRAM)
- CUDA 11.8+
- CONDA OR VIRTUALENV
- VS CODE + JUPYTER EXTENSION

---

## 🧪 CORE DEPENDENCIES

| CATEGORY | LIBRARY |
|--------|--------|
| DEEP LEARNING | TORCH, TORCHVISION |
| IMAGE PROCESSING | OPENCV-PYTHON |
| RESTORATION BACKBONE | BASICSr |
| FACE RESTORATION | GFPGAN |
| SUPER-RESOLUTION | REAL-ESRGAN |
| FACE DETECTION & ALIGNMENT | FACEXLIB |
| IDENTITY VERIFICATION | INSIGHTFACE |
| NUMERICAL COMPUTATION | NUMPY |
| VISUALIZATION | MATPLOTLIB |
| PERCEPTUAL METRICS | LPIPS |
| MODEL RUNTIME | ONNXRUNTIME / ONNXRUNTIME-GPU |
| UTILITIES | REQUESTS |

---

## 📦 INSTALLATION COMMANDS

> **RUN INSIDE YOUR VIRTUAL ENVIRONMENT**

```bash
# CORE DEEP LEARNING (CUDA 11.8)
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# IMAGE PROCESSING & NUMERICAL
pip install opencv-python numpy matplotlib

# RESTORATION TOOLBOX
pip install basicsr

# FACE DETECTION & RESTORATION
pip install facexlib gfpgan

# SUPER-RESOLUTION
pip install realesrgan

# IDENTITY VERIFICATION & METRICS
pip install insightface lpips

# MODEL RUNTIME (CHOOSE ONE)
pip install onnxruntime-gpu
# OR (CPU ONLY)
# pip install onnxruntime

# UTILITIES
pip install requests
```
---

## 🛠 BASICSR FALLBACK INSTALL (IF ERRORS OCCUR)

pip install -r `https://raw.githubusercontent.com/open-mmlab/basicsr/master/requirements.txt`

## ⚡ GPU ACCELERATION CHECK

```PYTHON
import torch
print(torch.cuda.is_available())
print(torch.cuda.get_device_name(0))
```

> EXPECTED OUTPUT:

```TEXT
TRUE
NVIDIA GPU NAME
```

## 🧼 BEST PRACTICES

✔ USE VIRTUAL ENVIRONMENTS <br>
✔ LOCK DEPENDENCY VERSIONS <br>
✔ ENABLE CUDA FOR ALL MODELS <br>
✔ TEST EACH MODULE IN ISOLATION <br>

## 🧠 NOTES

- ALL MODELS ARE USED IN INFERENCE MODE ONLY
- NO TRAINING REQUIRED
- FACEXLIB IS REQUIRED BY GFPGAN FOR FACE DETECTION
- INSIGHTFACE IS USED FOR IDENTITY CONSISTENCY VERIFICATION
- LPIPS IS OPTIONAL BUT RECOMMENDED FOR RESEARCH & EVALUATION
- DESIGNED FOR JUPYTER + VS CODE WORKFLOWS